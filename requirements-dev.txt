# Development dependencies (not needed at runtime)
ruff>=0.5.0
mypy>=1.10.0
pytest>=8.0.0
pytest-cov>=5.0.0
pre-commit>=3.0.0
build>=1.0.0
# httpx is required by fastapi.testclient / starlette.testclient for the server tests
httpx>=0.27.0
